import styled from "styled-components";

export const AppContainer = styled.div`
  margin: 0 auto;
`;

export const OrderItemsArea = styled.section`
  margin: 0 auto;
  max-width: 480px;

  display: flex;
  flex-wrap: wrap;
  justify-content: space-around;
  align-items: flex-end;
`;
